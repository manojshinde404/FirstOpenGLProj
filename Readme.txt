This is my First OpenGl project which i have done in college.
I have use ubuntu, OpenGl freeglut3 and C programming for this.
For Using this project need to install OpenGl freeglut libraries follow the below steps.

    step1: To create OpenGL development environment we need to get some necessary libraries including freeglut so, Open Terminal and enter the following commands:
          $ sudo apt install freeglut3-dev
    step2 : Run The file using below command(file name may diff)
          $ gcc filename.c -o gl -lGL -lGLU -lglut
   
